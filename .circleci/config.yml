version: 2.1

orbs:
  bitcartcc: bitcartcc/bitcartcc-shared@1.0.0

jobs:
  test:
    docker:
      - image: circleci/node:14

    working_directory: ~/repo

    steps:
      - checkout

      - restore_cache:
          keys:
            - v2-dependencies-{{ checksum "package.json" }}-{{ .Branch }}
            - v2-dependencies-

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v2-dependencies-{{ checksum "package.json" }}-{{ .Branch }}

      - run: yarn lint

workflows:
  version: 2
  test-and-deploy:
    jobs:
      - bitcartcc/deploy-amd64:
          name: deploy-amd64
          context: global
          pre-steps:
            - run: |
                export DOCKER_IMAGE=mrnaif/test-admin11
                export CIRCLE_TAG=0.6.0.0
      - bitcartcc/deploy-arm:
          name: deploy-arm
          context: global
          pre-steps:
            - run: |
                export DOCKER_IMAGE=mrnaif/test-admin11
                export CIRCLE_TAG=0.6.0.0
      - bitcartcc/deploy:
          name: deploy
          context: global
          requires:
            - deploy-amd64
            - deploy-arm
          pre-steps:
            - run: |
                export DOCKER_IMAGE=mrnaif/test-admin11
                export CIRCLE_TAG=0.6.0.0
